[project]
name = "pygrep"
version = "0.1.0"

dependencies = [
    "rich"
]

[project.scripts]
pygrep = "pygrep.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
